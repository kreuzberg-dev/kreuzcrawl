{
	"id": "error_dns_resolution",
	"description": "Handles DNS resolution failure gracefully",
	"tags": ["error", "network"],
	"skip": {
		"languages": ["wasm", "c"],
		"reason": "requires mock HTTP server; requires network; Requires real DNS resolution to fail"
	},
	"input": {
		"config": {
			"respect_robots_txt": false,
			"request_timeout": 5000
		}
	},
	"assertions": [
		{
			"type": "error"
		},
		{
			"type": "error",
			"value": "dns"
		}
	]
}
