{
	"id": "error_connection_refused",
	"description": "Handles connection refused error gracefully",
	"tags": ["error", "network"],
	"skip": {
		"languages": ["wasm", "c"],
		"reason": "requires mock HTTP server; Requires connection to actually be refused (port with no listener)"
	},
	"input": {
		"config": {
			"respect_robots_txt": false,
			"request_timeout_ms": 5000
		}
	},
	"assertions": [
		{
			"type": "error"
		},
		{
			"type": "error",
			"value": "connection"
		}
	]
}
