{
	"id": "sitemap_compressed_gzip",
	"description": "Parses a gzip-compressed sitemap file",
	"tags": ["sitemap", "compression"],
	"skip": {
		"languages": ["wasm", "c"],
		"reason": "requires mock HTTP server; Binary gzip body requires load_response_body_bytes helper; not yet supported by generator"
	},
	"input": {
		"mock_responses": [
			{
				"status_code": 200,
				"headers": {
					"content-type": "application/x-gzip"
				},
				"body_file": "xml/sitemap_gzip.xml.gz"
			}
		],
		"config": {
			"respect_robots_txt": false
		}
	},
	"assertions": [
		{
			"type": "equals",
			"field": "urls.length",
			"value": 3
		}
	]
}
