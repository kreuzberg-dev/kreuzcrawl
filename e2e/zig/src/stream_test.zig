// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:bbd5e5961aafa1ee6fb9288a9481189d3295028c86f9f5a211e3560fb7621bbb
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
const std = @import("std");
const testing = std.testing;
const kreuzcrawl = @import("kreuzcrawl");

// E2e tests for category: stream

test "crawl_stream_events" {
    // Crawl stream produces page and complete events
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_CRAWL_STREAM_EVENTS")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/crawl_stream_events", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = try kreuzcrawl.crawl_stream("{\"max_depth\":1,\"respect_robots_txt\":false}", url);
    defer std.heap.c_allocator.free(_result_json);
}

test "stream_depth_crawl" {
    // Stream produces events for multi-depth crawl with link following
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_STREAM_DEPTH_CRAWL")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/stream_depth_crawl", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = try kreuzcrawl.crawl_stream("{\"max_concurrent\":1,\"max_depth\":2}", url);
    defer std.heap.c_allocator.free(_result_json);
}

test "stream_error_event_mid_crawl" {
    // Stream emits error event when a page fails mid-crawl, but other pages succeed
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_STREAM_ERROR_EVENT_MID_CRAWL")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/stream_error_event_mid_crawl", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = try kreuzcrawl.crawl_stream("{\"max_concurrent\":1,\"max_depth\":1,\"respect_robots_txt\":false}", url);
    defer std.heap.c_allocator.free(_result_json);
}

test "stream_event_ordering" {
    // Stream ensures complete event arrives after all page events
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_STREAM_EVENT_ORDERING")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/stream_event_ordering", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = try kreuzcrawl.crawl_stream("{\"max_concurrent\":1,\"max_depth\":1,\"respect_robots_txt\":false}", url);
    defer std.heap.c_allocator.free(_result_json);
}

test "stream_large_crawl" {
    // Stream handles crawl of 5+ pages with multiple events
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_STREAM_LARGE_CRAWL")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/stream_large_crawl", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = try kreuzcrawl.crawl_stream("{\"max_depth\":1,\"respect_robots_txt\":false}", url);
    defer std.heap.c_allocator.free(_result_json);
}

test "stream_with_error_event" {
    // Stream emits page and complete events even when some pages fail
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_STREAM_WITH_ERROR_EVENT")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/stream_with_error_event", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = try kreuzcrawl.crawl_stream("{\"max_concurrent\":1,\"max_depth\":1}", url);
    defer std.heap.c_allocator.free(_result_json);
}
