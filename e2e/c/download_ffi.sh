#!/usr/bin/env bash
# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:3e58048fba4b7dd227a3a79a29ff4235240287b1348ce5610dfd496d9dd8a967
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
set -euo pipefail

REPO_URL="https://github.com/kreuzberg-dev/kreuzcrawl"
VERSION="0.3.0-rc.19"
FFI_PKG_NAME="kreuzcrawl-ffi"
FFI_DIR="ffi"

# Detect OS and architecture.
OS="$(uname -s | tr '[:upper:]' '[:lower:]')"
ARCH="$(uname -m)"

case "$ARCH" in
x86_64 | amd64) ARCH="x86_64" ;;
arm64 | aarch64) ARCH="aarch64" ;;
*)
  echo "Unsupported architecture: $ARCH" >&2
  exit 1
  ;;
esac

case "$OS" in
linux) TRIPLE="${ARCH}-unknown-linux-gnu" ;;
darwin) TRIPLE="${ARCH}-apple-darwin" ;;
*)
  echo "Unsupported OS: $OS" >&2
  exit 1
  ;;
esac

ARCHIVE="${FFI_PKG_NAME}-${TRIPLE}.tar.gz"
URL="${REPO_URL}/releases/download/v${VERSION}/${ARCHIVE}"

echo "Downloading ${ARCHIVE} from v${VERSION}..."
mkdir -p "$FFI_DIR"
curl -fSL "$URL" | tar xz -C "$FFI_DIR"
echo "FFI library extracted to $FFI_DIR/"
