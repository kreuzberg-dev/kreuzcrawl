/*
 * This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:86b68f9141de9efc9148a1fc4e15b8bb7c5ec83c9896f1ce92fc774ba2964a02
 * To regenerate: alef generate
 * To verify freshness: alef verify --exit-code
 * Issues & docs: https://github.com/kreuzberg-dev/alef
 */
/* E2e tests for category: scrape */

#include <assert.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "kreuzcrawl.h"
#include "test_runner.h"

void test_scrape_asset_dedup(void) {
    /* Same asset linked twice results in one download with one unique hash */
    KCRAWLScrape* result = scrape("{\"download_assets\":true}", NULL);
    assert(result != NULL && "expected call to succeed");
    char* status_code = kcrawl_scrape_status_code(result);
    KCRAWLAssets* assets_handle = kcrawl_scrape_assets(result);
    assert(assets_handle != NULL);
    char* assets_length = kcrawl_assets_length(assets_handle);
    char* assets_json = kcrawl_scrape_assets(result);
    assert(assets_json != NULL);
    char* assets__content_hash = alef_json_get_string(assets_json, "");
    assert(strcmp(status_code, 200) == 0 && "equals assertion failed");
    assert(strcmp(assets_length, 2) == 0 && "equals assertion failed");
    assert(assets__content_hash != NULL && strlen(assets__content_hash) > 0 && "expected non-empty value");
    kcrawl_free_string(status_code);
    kcrawl_free_string(assets_length);
    free(assets__content_hash);
    kcrawl_free_string(assets_json);
    kcrawl_assets_free(assets_handle);
    kcrawl_scrape_free(result);
}

void test_scrape_asset_max_size(void) {
    /* Skips assets exceeding max_asset_size limit */
    KCRAWLScrape* result = scrape("{\"download_assets\":true,\"max_asset_size\":150}", NULL);
    assert(result != NULL && "expected call to succeed");
    char* status_code = kcrawl_scrape_status_code(result);
    KCRAWLAssets* assets_handle = kcrawl_scrape_assets(result);
    assert(assets_handle != NULL);
    char* assets_length = kcrawl_assets_length(assets_handle);
    assert(strcmp(status_code, 200) == 0 && "equals assertion failed");
    assert(strcmp(assets_length, 2) == 0 && "equals assertion failed");
    kcrawl_free_string(status_code);
    kcrawl_free_string(assets_length);
    kcrawl_assets_free(assets_handle);
    kcrawl_scrape_free(result);
}

void test_scrape_asset_type_filter(void) {
    /* Only downloads image assets when asset_types filter is set */
    KCRAWLScrape* result = scrape("{\"asset_types\":[\"image\"],\"download_assets\":true}", NULL);
    assert(result != NULL && "expected call to succeed");
    char* status_code = kcrawl_scrape_status_code(result);
    KCRAWLAssets* assets_handle = kcrawl_scrape_assets(result);
    assert(assets_handle != NULL);
    char* assets_length = kcrawl_assets_length(assets_handle);
    char* assets_json = kcrawl_scrape_assets(result);
    assert(assets_json != NULL);
    char* assets__category = alef_json_get_string(assets_json, "");
    assert(strcmp(status_code, 200) == 0 && "equals assertion failed");
    assert(strcmp(assets_length, 1) == 0 && "equals assertion failed");
    assert(strstr(assets__category, "image") != NULL && "expected to contain substring");
    kcrawl_free_string(status_code);
    kcrawl_free_string(assets_length);
    free(assets__category);
    kcrawl_free_string(assets_json);
    kcrawl_assets_free(assets_handle);
    kcrawl_scrape_free(result);
}

void test_scrape_basic_html_page(void) {
    /* Scrapes a simple HTML page and extracts title, description, and links */
    KCRAWLScrape* result = scrape("{\"max_depth\":0,\"respect_robots_txt\":false}", NULL);
    assert(result != NULL && "expected call to succeed");
    char* status_code = kcrawl_scrape_status_code(result);
    char* content_type = kcrawl_scrape_content_type(result);
    char* html = kcrawl_scrape_html(result);
    KCRAWLMetadata* metadata_handle = kcrawl_scrape_metadata(result);
    assert(metadata_handle != NULL);
    char* metadata_title = kcrawl_metadata_title(metadata_handle);
    char* metadata_description = kcrawl_metadata_description(metadata_handle);
    char* metadata_canonical_url = kcrawl_metadata_canonical_url(metadata_handle);
    KCRAWLLinks* links_handle = kcrawl_scrape_links(result);
    assert(links_handle != NULL);
    char* links_length = kcrawl_links_length(links_handle);
    char* links_json = kcrawl_scrape_links(result);
    assert(links_json != NULL);
    char* links__link_type = alef_json_get_string(links_json, "");
    KCRAWLImages* images_handle = kcrawl_scrape_images(result);
    assert(images_handle != NULL);
    char* images_length = kcrawl_images_length(images_handle);
    char* og_title = kcrawl_metadata_og_title(metadata_handle);
    assert(strcmp(status_code, 200) == 0 && "equals assertion failed");
    assert(str_trim_eq(content_type, "text/html") == 0 && "equals assertion failed");
    assert(html != NULL && strlen(html) > 0 && "expected non-empty value");
    assert(str_trim_eq(metadata_title, "Example Domain") == 0 && "equals assertion failed");
    assert(strstr(metadata_description, "illustrative examples") != NULL && "expected to contain substring");
    assert(metadata_canonical_url != NULL && strlen(metadata_canonical_url) > 0 && "expected non-empty value");
    assert(links_length > 0 && "expected greater than");
    assert(strstr(links__link_type, "external") != NULL && "expected to contain substring");
    assert(strcmp(images_length, 0) == 0 && "equals assertion failed");
    assert(strlen(og_title) == 0 && "expected empty value");
    kcrawl_free_string(status_code);
    kcrawl_free_string(content_type);
    kcrawl_free_string(html);
    kcrawl_free_string(metadata_title);
    kcrawl_free_string(metadata_description);
    kcrawl_free_string(metadata_canonical_url);
    kcrawl_free_string(links_length);
    free(links__link_type);
    kcrawl_free_string(images_length);
    kcrawl_free_string(og_title);
    kcrawl_images_free(images_handle);
    kcrawl_free_string(links_json);
    kcrawl_links_free(links_handle);
    kcrawl_metadata_free(metadata_handle);
    kcrawl_scrape_free(result);
}

void test_scrape_complex_links(void) {
    /* Classifies links by type: internal, external, anchor, document, image */
    KCRAWLScrape* result = scrape("{}", NULL);
    assert(result != NULL && "expected call to succeed");
    char* status_code = kcrawl_scrape_status_code(result);
    KCRAWLLinks* links_handle = kcrawl_scrape_links(result);
    assert(links_handle != NULL);
    char* links_length = kcrawl_links_length(links_handle);
    char* links_json = kcrawl_scrape_links(result);
    assert(links_json != NULL);
    char* links__url = alef_json_get_string(links_json, "");
    assert(strcmp(status_code, 200) == 0 && "equals assertion failed");
    assert(links_length > 9 && "expected greater than");
    assert(links__url != NULL && strlen(links__url) > 0 && "expected non-empty value");
    kcrawl_free_string(status_code);
    kcrawl_free_string(links_length);
    free(links__url);
    kcrawl_free_string(links_json);
    kcrawl_links_free(links_handle);
    kcrawl_scrape_free(result);
}

void test_scrape_download_assets(void) {
    /* Downloads CSS, JS, and image assets from page */
    KCRAWLScrape* result = scrape("{\"download_assets\":true}", NULL);
    assert(result != NULL && "expected call to succeed");
    char* status_code = kcrawl_scrape_status_code(result);
    KCRAWLAssets* assets_handle = kcrawl_scrape_assets(result);
    assert(assets_handle != NULL);
    char* assets_length = kcrawl_assets_length(assets_handle);
    assert(strcmp(status_code, 200) == 0 && "equals assertion failed");
    assert(assets_length > 2 && "expected greater than");
    kcrawl_free_string(status_code);
    kcrawl_free_string(assets_length);
    kcrawl_assets_free(assets_handle);
    kcrawl_scrape_free(result);
}

void test_scrape_dublin_core(void) {
    /* Extracts Dublin Core metadata from a page */
    KCRAWLScrape* result = scrape("{}", NULL);
    assert(result != NULL && "expected call to succeed");
    char* status_code = kcrawl_scrape_status_code(result);
    KCRAWLMetadata* metadata_handle = kcrawl_scrape_metadata(result);
    assert(metadata_handle != NULL);
    char* dublin_core_title = kcrawl_metadata_dc_title(metadata_handle);
    char* dublin_core_creator = kcrawl_metadata_dc_creator(metadata_handle);
    assert(strcmp(status_code, 200) == 0 && "equals assertion failed");
    assert(dublin_core_title != NULL && strlen(dublin_core_title) > 0 && "expected non-empty value");
    assert(str_trim_eq(dublin_core_title, "Effects of Climate Change on Marine Biodiversity") == 0 && "equals assertion failed");
    assert(str_trim_eq(dublin_core_creator, "Dr. Jane Smith") == 0 && "equals assertion failed");
    kcrawl_free_string(status_code);
    kcrawl_free_string(dublin_core_title);
    kcrawl_free_string(dublin_core_creator);
    kcrawl_metadata_free(metadata_handle);
    kcrawl_scrape_free(result);
}

void test_scrape_empty_page(void) {
    /* Handles an empty HTML document without errors */
    KCRAWLScrape* result = scrape("{}", NULL);
    assert(result != NULL && "expected call to succeed");
    char* status_code = kcrawl_scrape_status_code(result);
    KCRAWLLinks* links_handle = kcrawl_scrape_links(result);
    assert(links_handle != NULL);
    char* links_length = kcrawl_links_length(links_handle);
    KCRAWLImages* images_handle = kcrawl_scrape_images(result);
    assert(images_handle != NULL);
    char* images_length = kcrawl_images_length(images_handle);
    assert(strcmp(status_code, 200) == 0 && "equals assertion failed");
    assert(links_length > -1 && "expected greater than");
    assert(strcmp(images_length, 0) == 0 && "equals assertion failed");
    kcrawl_free_string(status_code);
    kcrawl_free_string(links_length);
    kcrawl_free_string(images_length);
    kcrawl_images_free(images_handle);
    kcrawl_links_free(links_handle);
    kcrawl_scrape_free(result);
}

void test_scrape_feed_discovery(void) {
    /* Discovers RSS, Atom, and JSON feed links */
    KCRAWLScrape* result = scrape("{}", NULL);
    assert(result != NULL && "expected call to succeed");
    char* status_code = kcrawl_scrape_status_code(result);
    KCRAWLFeeds* feeds_handle = kcrawl_scrape_feeds(result);
    assert(feeds_handle != NULL);
    char* feeds_length = kcrawl_feeds_length(feeds_handle);
    assert(strcmp(status_code, 200) == 0 && "equals assertion failed");
    assert(feeds_length >= 3 && "expected greater than or equal");
    kcrawl_free_string(status_code);
    kcrawl_free_string(feeds_length);
    kcrawl_feeds_free(feeds_handle);
    kcrawl_scrape_free(result);
}

void test_scrape_image_sources(void) {
    /* Extracts images from img, picture, og:image, twitter:image */
    KCRAWLScrape* result = scrape("{}", NULL);
    assert(result != NULL && "expected call to succeed");
    char* status_code = kcrawl_scrape_status_code(result);
    KCRAWLImages* images_handle = kcrawl_scrape_images(result);
    assert(images_handle != NULL);
    char* images_length = kcrawl_images_length(images_handle);
    KCRAWLMetadata* metadata_handle = kcrawl_scrape_metadata(result);
    assert(metadata_handle != NULL);
    char* og_image = kcrawl_metadata_og_image(metadata_handle);
    assert(strcmp(status_code, 200) == 0 && "equals assertion failed");
    assert(images_length > 4 && "expected greater than");
    assert(str_trim_eq(og_image, "https://example.com/images/og-hero.jpg") == 0 && "equals assertion failed");
    kcrawl_free_string(status_code);
    kcrawl_free_string(images_length);
    kcrawl_free_string(og_image);
    kcrawl_metadata_free(metadata_handle);
    kcrawl_images_free(images_handle);
    kcrawl_scrape_free(result);
}

void test_scrape_js_heavy_spa(void) {
    /* Handles SPA page with JavaScript-only content (no server-rendered HTML) */
    KCRAWLScrape* result = scrape("{}", NULL);
    assert(result != NULL && "expected call to succeed");
    char* html = kcrawl_scrape_html(result);
    assert(html != NULL && strlen(html) > 0 && "expected non-empty value");
    kcrawl_free_string(html);
    kcrawl_scrape_free(result);
}

void test_scrape_json_ld(void) {
    /* Extracts JSON-LD structured data from a page */
    KCRAWLScrape* result = scrape("{}", NULL);
    assert(result != NULL && "expected call to succeed");
    char* status_code = kcrawl_scrape_status_code(result);
    char* json_ld = kcrawl_scrape_json_ld(result);
    char* json_ld_json = kcrawl_scrape_json_ld(result);
    assert(json_ld_json != NULL);
    char* json_ld_type = alef_json_get_string(json_ld_json, "");
    KCRAWLJsonLd* json_ld_handle = kcrawl_scrape_json_ld(result);
    assert(json_ld_handle != NULL);
    char* json_ld_name = kcrawl_json_ld_name(json_ld_handle);
    assert(strcmp(status_code, 200) == 0 && "equals assertion failed");
    assert(json_ld != NULL && strlen(json_ld) > 0 && "expected non-empty value");
    assert(str_trim_eq(json_ld_type, "Recipe") == 0 && "equals assertion failed");
    assert(str_trim_eq(json_ld_name, "Best Chocolate Cake") == 0 && "equals assertion failed");
    kcrawl_free_string(status_code);
    kcrawl_free_string(json_ld);
    free(json_ld_type);
    kcrawl_free_string(json_ld_name);
    kcrawl_json_ld_free(json_ld_handle);
    kcrawl_free_string(json_ld_json);
    kcrawl_scrape_free(result);
}

void test_scrape_malformed_html(void) {
    /* Gracefully handles broken HTML without crashing */
    KCRAWLScrape* result = scrape("{}", NULL);
    assert(result != NULL && "expected call to succeed");
    char* status_code = kcrawl_scrape_status_code(result);
    char* html = kcrawl_scrape_html(result);
    KCRAWLMetadata* metadata_handle = kcrawl_scrape_metadata(result);
    assert(metadata_handle != NULL);
    char* metadata_description = kcrawl_metadata_description(metadata_handle);
    assert(strcmp(status_code, 200) == 0 && "equals assertion failed");
    assert(html != NULL && strlen(html) > 0 && "expected non-empty value");
    assert(strstr(metadata_description, "broken HTML") != NULL && "expected to contain substring");
    kcrawl_free_string(status_code);
    kcrawl_free_string(html);
    kcrawl_free_string(metadata_description);
    kcrawl_metadata_free(metadata_handle);
    kcrawl_scrape_free(result);
}

void test_scrape_og_metadata(void) {
    /* Extracts full Open Graph metadata from a page */
    KCRAWLScrape* result = scrape("{}", NULL);
    assert(result != NULL && "expected call to succeed");
    char* status_code = kcrawl_scrape_status_code(result);
    KCRAWLMetadata* metadata_handle = kcrawl_scrape_metadata(result);
    assert(metadata_handle != NULL);
    char* og_title = kcrawl_metadata_og_title(metadata_handle);
    char* og_type = kcrawl_metadata_og_type(metadata_handle);
    char* og_image = kcrawl_metadata_og_image(metadata_handle);
    char* og_description = kcrawl_metadata_og_description(metadata_handle);
    char* metadata_title = kcrawl_metadata_title(metadata_handle);
    assert(strcmp(status_code, 200) == 0 && "equals assertion failed");
    assert(og_title != NULL && strlen(og_title) > 0 && "expected non-empty value");
    assert(str_trim_eq(og_title, "Article Title") == 0 && "equals assertion failed");
    assert(str_trim_eq(og_type, "article") == 0 && "equals assertion failed");
    assert(str_trim_eq(og_image, "https://example.com/images/article-hero.jpg") == 0 && "equals assertion failed");
    assert(og_description != NULL && strlen(og_description) > 0 && "expected non-empty value");
    assert(str_trim_eq(metadata_title, "Article Title - Example Blog") == 0 && "equals assertion failed");
    kcrawl_free_string(status_code);
    kcrawl_free_string(og_title);
    kcrawl_free_string(og_type);
    kcrawl_free_string(og_image);
    kcrawl_free_string(og_description);
    kcrawl_free_string(metadata_title);
    kcrawl_metadata_free(metadata_handle);
    kcrawl_scrape_free(result);
}

void test_scrape_twitter_card(void) {
    /* Extracts Twitter Card metadata from a page */
    KCRAWLScrape* result = scrape("{}", NULL);
    assert(result != NULL && "expected call to succeed");
    char* status_code = kcrawl_scrape_status_code(result);
    KCRAWLMetadata* metadata_handle = kcrawl_scrape_metadata(result);
    assert(metadata_handle != NULL);
    char* twitter_card = kcrawl_metadata_twitter_card(metadata_handle);
    char* twitter_card_type = kcrawl_metadata_twitter_card(metadata_handle);
    char* twitter_title = kcrawl_metadata_twitter_title(metadata_handle);
    assert(strcmp(status_code, 200) == 0 && "equals assertion failed");
    assert(twitter_card != NULL && strlen(twitter_card) > 0 && "expected non-empty value");
    assert(str_trim_eq(twitter_card_type, "summary_large_image") == 0 && "equals assertion failed");
    assert(str_trim_eq(twitter_title, "New Product Launch") == 0 && "equals assertion failed");
    kcrawl_free_string(status_code);
    kcrawl_free_string(twitter_card);
    kcrawl_free_string(twitter_card_type);
    kcrawl_free_string(twitter_title);
    kcrawl_metadata_free(metadata_handle);
    kcrawl_scrape_free(result);
}
