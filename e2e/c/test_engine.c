/*
 * This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:4bac9539cba282c34796dd77419e2dfa7473db648acb224046187d10e2fba25b
 * To regenerate: alef generate
 * To verify freshness: alef verify --exit-code
 * Issues & docs: https://github.com/kreuzberg-dev/alef
 */
/* E2e tests for category: engine */

#include <assert.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "kreuzcrawl.h"
#include "test_runner.h"

void test_engine_batch_basic(void) {
    /* CrawlEngine with defaults batch scrapes like the free function */
    KCRAWLScrape* result = scrape("{}", NULL);
    assert(result != NULL && "expected call to succeed");
    KCRAWLBatch* batch_handle = kcrawl_scrape_batch(result);
    assert(batch_handle != NULL);
    char* batch_completed_count = kcrawl_batch_completed_count(batch_handle);
    char* batch_total_count = kcrawl_batch_total_count(batch_handle);
    // skipped: field 'batch.completed_count' not available on result type
    // skipped: field 'batch.total_count' not available on result type
    kcrawl_free_string(batch_completed_count);
    kcrawl_free_string(batch_total_count);
    kcrawl_batch_free(batch_handle);
    kcrawl_scrape_free(result);
}

void test_engine_crawl_basic(void) {
    /* CrawlEngine with defaults crawls multiple pages like the free function */
    KCRAWLScrape* result = scrape("{\"max_depth\":1}", NULL);
    assert(result != NULL && "expected call to succeed");
    KCRAWLCrawl* crawl_handle = kcrawl_scrape_crawl(result);
    assert(crawl_handle != NULL);
    char* crawl_pages_crawled = kcrawl_crawl_pages_crawled(crawl_handle);
    char* crawl_min_pages = kcrawl_crawl_min_pages(crawl_handle);
    // skipped: field 'crawl.pages_crawled' not available on result type
    // skipped: field 'crawl.min_pages' not available on result type
    kcrawl_free_string(crawl_pages_crawled);
    kcrawl_free_string(crawl_min_pages);
    kcrawl_crawl_free(crawl_handle);
    kcrawl_scrape_free(result);
}

void test_engine_map_basic(void) {
    /* CrawlEngine with defaults discovers URLs like the free function */
    KCRAWLScrape* result = scrape("{}", NULL);
    assert(result != NULL && "expected call to succeed");
    KCRAWLMap* map_handle = kcrawl_scrape_map(result);
    assert(map_handle != NULL);
    char* map_min_urls = kcrawl_map_min_urls(map_handle);
    // skipped: field 'map.min_urls' not available on result type
    kcrawl_free_string(map_min_urls);
    kcrawl_map_free(map_handle);
    kcrawl_scrape_free(result);
}

void test_engine_scrape_basic(void) {
    /* CrawlEngine with defaults scrapes a page identically to the free function */
    KCRAWLScrape* result = scrape("{}", NULL);
    assert(result != NULL && "expected call to succeed");
    char* status_code = kcrawl_scrape_status_code(result);
    char* content_type = kcrawl_scrape_content_type(result);
    KCRAWLMetadata* metadata_handle = kcrawl_scrape_metadata(result);
    assert(metadata_handle != NULL);
    char* metadata_title = kcrawl_metadata_title(metadata_handle);
    char* metadata_description = kcrawl_metadata_description(metadata_handle);
    KCRAWLLinks* links_handle = kcrawl_scrape_links(result);
    assert(links_handle != NULL);
    char* links_length = kcrawl_links_length(links_handle);
    KCRAWLHeadings* headings_handle = kcrawl_metadata_headings(metadata_handle);
    assert(headings_handle != NULL);
    char* metadata_headings_length = kcrawl_headings_length(headings_handle);
    assert(strcmp(status_code, 200) == 0 && "equals assertion failed");
    assert(str_trim_eq(content_type, "text/html") == 0 && "equals assertion failed");
    assert(str_trim_eq(metadata_title, "Engine Test") == 0 && "equals assertion failed");
    assert(strstr(metadata_description, "Testing the engine") != NULL && "expected to contain substring");
    assert(links_length >= 1 && "expected greater than or equal");
    assert(metadata_headings_length >= 1 && "expected greater than or equal");
    kcrawl_free_string(status_code);
    kcrawl_free_string(content_type);
    kcrawl_free_string(metadata_title);
    kcrawl_free_string(metadata_description);
    kcrawl_free_string(links_length);
    kcrawl_free_string(metadata_headings_length);
    kcrawl_headings_free(headings_handle);
    kcrawl_links_free(links_handle);
    kcrawl_metadata_free(metadata_handle);
    kcrawl_scrape_free(result);
}

void test_engine_stream_basic(void) {
    /* CrawlEngine with defaults streams events like the free function */
    KCRAWLScrape* result = scrape("{\"max_depth\":1}", NULL);
    assert(result != NULL && "expected call to succeed");
    KCRAWLStream* stream_handle = kcrawl_scrape_stream(result);
    assert(stream_handle != NULL);
    char* stream_has_page_event = kcrawl_stream_has_page_event(stream_handle);
    char* stream_has_complete_event = kcrawl_stream_has_complete_event(stream_handle);
    char* stream_event_count_min = kcrawl_stream_event_count_min(stream_handle);
    // skipped: field 'stream.has_page_event' not available on result type
    // skipped: field 'stream.has_complete_event' not available on result type
    // skipped: field 'stream.event_count_min' not available on result type
    kcrawl_free_string(stream_has_page_event);
    kcrawl_free_string(stream_has_complete_event);
    kcrawl_free_string(stream_event_count_min);
    kcrawl_stream_free(stream_handle);
    kcrawl_scrape_free(result);
}
