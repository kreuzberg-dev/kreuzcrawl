/*
 * This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:b7f9fc10a05f8353ed7575f64cb061888ed0630119aaf84d6581d220d7d2e01a
 * To regenerate: alef generate
 * To verify freshness: alef verify --exit-code
 * Issues & docs: https://github.com/kreuzberg-dev/alef
 */
/* E2e tests for category: markdown */

#include <assert.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "kreuzcrawl.h"
#include "test_runner.h"

void test_markdown_basic_conversion(void) {
    /* HTML is always converted to markdown alongside raw HTML */
    KCRAWLScrape* result = scrape("{}", NULL);
    assert(result != NULL && "expected call to succeed");
    char* status_code = kcrawl_scrape_status_code(result);
    KCRAWLMetadata* metadata_handle = kcrawl_scrape_metadata(result);
    assert(metadata_handle != NULL);
    char* metadata_title = kcrawl_metadata_title(metadata_handle);
    char* html = kcrawl_scrape_html(result);
    KCRAWLMarkdown* markdown_handle = kcrawl_scrape_markdown(result);
    assert(markdown_handle != NULL);
    char* markdown_content = kcrawl_markdown_content(markdown_handle);
    assert(strcmp(status_code, 200) == 0 && "equals assertion failed");
    assert(str_trim_eq(metadata_title, "Test") == 0 && "equals assertion failed");
    assert(html != NULL && strlen(html) > 0 && "expected non-empty value");
    assert(markdown_content != NULL && strlen(markdown_content) > 0 && "expected non-empty value");
    assert(strstr(markdown_content, "Hello World") != NULL && "expected to contain substring");
    kcrawl_free_string(status_code);
    kcrawl_free_string(metadata_title);
    kcrawl_free_string(html);
    kcrawl_free_string(markdown_content);
    kcrawl_markdown_free(markdown_handle);
    kcrawl_metadata_free(metadata_handle);
    kcrawl_scrape_free(result);
}

void test_markdown_crawl_all_pages(void) {
    /* All crawled pages have markdown field populated */
    KCRAWLScrape* result = scrape("{\"max_depth\":1}", NULL);
    assert(result != NULL && "expected call to succeed");
    KCRAWLCrawl* crawl_handle = kcrawl_scrape_crawl(result);
    assert(crawl_handle != NULL);
    char* crawl_pages_crawled = kcrawl_crawl_pages_crawled(crawl_handle);
    // skipped: field 'crawl.pages_crawled' not available on result type
    kcrawl_free_string(crawl_pages_crawled);
    kcrawl_crawl_free(crawl_handle);
    kcrawl_scrape_free(result);
}

void test_markdown_fit_content(void) {
    /* Fit markdown removes navigation and boilerplate content */
    KCRAWLScrape* result = scrape("{}", NULL);
    assert(result != NULL && "expected call to succeed");
    char* status_code = kcrawl_scrape_status_code(result);
    KCRAWLMarkdown* markdown_handle = kcrawl_scrape_markdown(result);
    assert(markdown_handle != NULL);
    char* markdown_content = kcrawl_markdown_content(markdown_handle);
    assert(strcmp(status_code, 200) == 0 && "equals assertion failed");
    assert(markdown_content != NULL && strlen(markdown_content) > 0 && "expected non-empty value");
    kcrawl_free_string(status_code);
    kcrawl_free_string(markdown_content);
    kcrawl_markdown_free(markdown_handle);
    kcrawl_scrape_free(result);
}

void test_markdown_headings_and_paragraphs(void) {
    /* Markdown conversion preserves heading hierarchy and paragraph text */
    KCRAWLScrape* result = scrape("{}", NULL);
    assert(result != NULL && "expected call to succeed");
    KCRAWLMarkdown* markdown_handle = kcrawl_scrape_markdown(result);
    assert(markdown_handle != NULL);
    char* markdown_content = kcrawl_markdown_content(markdown_handle);
    assert(markdown_content != NULL && strlen(markdown_content) > 0 && "expected non-empty value");
    assert(strstr(markdown_content, "Main Title") != NULL && "expected to contain substring");
    kcrawl_free_string(markdown_content);
    kcrawl_markdown_free(markdown_handle);
    kcrawl_scrape_free(result);
}

void test_markdown_links_converted(void) {
    /* HTML links are converted to markdown link syntax */
    KCRAWLScrape* result = scrape("{}", NULL);
    assert(result != NULL && "expected call to succeed");
    char* status_code = kcrawl_scrape_status_code(result);
    char* html = kcrawl_scrape_html(result);
    KCRAWLMarkdown* markdown_handle = kcrawl_scrape_markdown(result);
    assert(markdown_handle != NULL);
    char* markdown_content = kcrawl_markdown_content(markdown_handle);
    assert(strcmp(status_code, 200) == 0 && "equals assertion failed");
    assert(html != NULL && strlen(html) > 0 && "expected non-empty value");
    assert(markdown_content != NULL && strlen(markdown_content) > 0 && "expected non-empty value");
    assert(strstr(markdown_content, "Example") != NULL && "expected to contain substring");
    kcrawl_free_string(status_code);
    kcrawl_free_string(html);
    kcrawl_free_string(markdown_content);
    kcrawl_markdown_free(markdown_handle);
    kcrawl_scrape_free(result);
}

void test_markdown_with_citations(void) {
    /* Markdown includes citation conversion with numbered references */
    KCRAWLScrape* result = scrape("{}", NULL);
    assert(result != NULL && "expected call to succeed");
    char* status_code = kcrawl_scrape_status_code(result);
    KCRAWLMarkdown* markdown_handle = kcrawl_scrape_markdown(result);
    assert(markdown_handle != NULL);
    char* markdown_content = kcrawl_markdown_content(markdown_handle);
    assert(strcmp(status_code, 200) == 0 && "equals assertion failed");
    assert(markdown_content != NULL && strlen(markdown_content) > 0 && "expected non-empty value");
    kcrawl_free_string(status_code);
    kcrawl_free_string(markdown_content);
    kcrawl_markdown_free(markdown_handle);
    kcrawl_scrape_free(result);
}
