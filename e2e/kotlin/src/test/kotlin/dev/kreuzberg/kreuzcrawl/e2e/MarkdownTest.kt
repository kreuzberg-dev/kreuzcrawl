// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:7289d5c40554a3bd979d8464d6b26c3cffb44651f4bd8acc7a9795a4f4ac3e05
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.kreuzcrawl.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import dev.kreuzberg.kreuzcrawl.Kreuzcrawl
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import dev.kreuzberg.kreuzcrawl.CrawlConfig

/** E2e tests for category: markdown. */
class MarkdownTest {

    companion object {
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module()).setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }
    @Test
    fun testMarkdownBasicConversion() {
        // HTML is always converted to markdown alongside raw HTML
        val engine = Kreuzcrawl.createEngine(null)
        val url = System.getenv("MOCK_SERVER_URL") + "/fixtures/markdown_basic_conversion"
        val result = Kreuzcrawl.scrape(engine, url)
        assertEquals(200, result.statusCode())
        assertEquals("Test", result.metadata().title().orEmpty().trim())
        assertFalse(result.html().isEmpty(), "expected non-empty value")
        assertTrue(result.markdown()?.content() != null, "expected non-empty value")
        assertTrue(result.markdown()?.content().orEmpty().contains("Hello World"), "expected to contain: " + "Hello World")
    }

    @Test
    fun testMarkdownCrawlAllPages() {
        // All crawled pages have markdown field populated
        val engineConfig = MAPPER.readValue("{\"max_depth\":1}", CrawlConfig::class.java)
        val engine = Kreuzcrawl.createEngine(engineConfig)
        val url = System.getenv("MOCK_SERVER_URL") + "/fixtures/markdown_crawl_all_pages"
        val result = Kreuzcrawl.scrape(engine, url)
        // skipped: field 'crawl.pages_crawled' not available on result type
    }

    @Test
    fun testMarkdownFitContent() {
        // Fit markdown removes navigation and boilerplate content
        val engine = Kreuzcrawl.createEngine(null)
        val url = System.getenv("MOCK_SERVER_URL") + "/fixtures/markdown_fit_content"
        val result = Kreuzcrawl.scrape(engine, url)
        assertEquals(200, result.statusCode())
        assertTrue(result.markdown()?.content() != null, "expected non-empty value")
    }

    @Test
    fun testMarkdownHeadingsAndParagraphs() {
        // Markdown conversion preserves heading hierarchy and paragraph text
        val engine = Kreuzcrawl.createEngine(null)
        val url = System.getenv("MOCK_SERVER_URL") + "/fixtures/markdown_headings_and_paragraphs"
        val result = Kreuzcrawl.scrape(engine, url)
        assertTrue(result.markdown()?.content() != null, "expected non-empty value")
        assertTrue(result.markdown()?.content().orEmpty().contains("Main Title"), "expected to contain: " + "Main Title")
    }

    @Test
    fun testMarkdownLinksConverted() {
        // HTML links are converted to markdown link syntax
        val engine = Kreuzcrawl.createEngine(null)
        val url = System.getenv("MOCK_SERVER_URL") + "/fixtures/markdown_links_converted"
        val result = Kreuzcrawl.scrape(engine, url)
        assertEquals(200, result.statusCode())
        assertFalse(result.html().isEmpty(), "expected non-empty value")
        assertTrue(result.markdown()?.content() != null, "expected non-empty value")
        assertTrue(result.markdown()?.content().orEmpty().contains("Example"), "expected to contain: " + "Example")
    }

    @Test
    fun testMarkdownWithCitations() {
        // Markdown includes citation conversion with numbered references
        val engine = Kreuzcrawl.createEngine(null)
        val url = System.getenv("MOCK_SERVER_URL") + "/fixtures/markdown_with_citations"
        val result = Kreuzcrawl.scrape(engine, url)
        assertEquals(200, result.statusCode())
        assertTrue(result.markdown()?.content() != null, "expected non-empty value")
    }

}
